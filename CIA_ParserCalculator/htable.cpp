/*
#include "htable.h"

const List& HTable::find(const char* str) const {
	int i = hash(str);
	return _aList[i];
}

void HTable::add(const char* str, int id) {
	int i = hash(str);
	_aList[i].add(id);
}
*/