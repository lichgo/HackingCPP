#include "parser.h"
#include "symbol_table.h"

#include <iostream>

using namespace std;

const int maxBuf = 100;

void main_parser_calcualtor() {
	//char buf[maxBuf];
	//Status status;
	//SymbolTable symTab;

	//do {
	//	cout << ">  ";
	//	cin.getline(buf, maxBuf);

	//	Scanner scanner(buf);
	//	Parser parser(scanner, symTab);

	//	status = parser.eval();
	//} while (status != QUIT);
}
